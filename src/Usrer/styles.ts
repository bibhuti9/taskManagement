import {StyleSheet} from 'react-native';
export const styles = StyleSheet.create({
  container: {
    borderWidth: 1,
    borderRadius: 20,
    padding: 5,
    margin: 5,
    backgroundColor: 'gold',
  },
});
